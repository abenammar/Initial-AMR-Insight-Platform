# Contributing

1. Create a focused branch.
2. Add or update tests for backend behaviour.
3. Run `mvn test` in `backend` and `npm run build` in `frontend`.
4. Do not commit real patient data, credentials, or institution identifiers.
5. Describe database migration and API compatibility impacts in the pull request.
