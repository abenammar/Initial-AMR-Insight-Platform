# Validation record

- Frontend TypeScript compilation and Vite production build completed successfully in the generation environment.
- JSON, XML, and YAML files were syntax-checked.
- Backend source includes Maven tests and a GitHub Actions workflow. Run `scripts/validate.sh` in an environment with Maven, Node.js, and network access to perform the complete build.
